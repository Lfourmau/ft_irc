#include "bot.hpp"

int main(int argc, char** argv)
{
	std::string	server;
	std::string	port;
	std::string	nick;
	std::string	channel;
	std::string	buffer;
	int			msg_type;

	std::stringstream ss;
	ss << ;
	users = ss.str();

	if (argc != 4) {
		std::cerr << "Usage: ./ircbot <server> <port> <password>" << std::endl;
		exit(-1);
	}
	server = argv[1];
	port = argv[2];
	

}